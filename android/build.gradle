buildscript {
  ext.ReactNativePrinterKit = [
    kotlinVersion: "2.0.21",
    minSdkVersion: 24,
    compileSdkVersion: 36
  ]

  ext.getExtOrDefault = { prop ->
    if (rootProject.ext.has(prop)) {
      return rootProject.ext.get(prop)
    }

    return ReactNativePrinterKit[prop]
  }

  repositories {
    google()
    mavenCentral()
    maven { url = uri("https://jitpack.io") }
  }

  dependencies {
    classpath "com.android.tools.build:gradle:8.7.2"
    // noinspection DifferentKotlinGradleVersion
    classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${getExtOrDefault('kotlinVersion')}"
  }
}


apply plugin: "com.android.library"

if (project.extensions.findByName("kotlin") == null) {
  apply plugin: "kotlin-android"
}


apply plugin: "com.facebook.react"

android {
  namespace "com.rejaul.reactnativeprinterkit"

  compileSdkVersion getExtOrDefault("compileSdkVersion")

  defaultConfig {
    minSdkVersion getExtOrDefault("minSdkVersion")
  }

  compileOptions {
    sourceCompatibility JavaVersion.VERSION_17
    targetCompatibility JavaVersion.VERSION_17
  }
}

repositories {
  google()
  mavenCentral()
  // JitPack resolves this multi-module repo by its GitHub repo name
  // (android-printer-kit), not the internal Gradle module name (printerkit).
  maven { url = uri("https://jitpack.io") }
}

dependencies {
  implementation "com.facebook.react:react-android"
  implementation("com.github.DeveloperRejaul:android-printer-kit:v0.0.7")
}
